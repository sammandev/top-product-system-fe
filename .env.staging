# Frontend Staging Environment Variables
# Used by Vite build process and runtime

# API Base URL - empty because nginx proxy handles /api/ routing
# Frontend code already includes /api in paths
VITE_API_BASE_URL=

# Application Title
VITE_APP_TITLE=AST Tools

# Environment identifier
VITE_ENVIRONMENT=staging
